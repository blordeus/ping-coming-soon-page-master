const form = document.querySelector('form');
const burron = form.querySelector('[type="submit"]');

function checkEmail (e) {
    e.preventDefault();
    const email = e.target.querySelector('[ty]');

}